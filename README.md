# Raycasting-Project
What you need
1. Install python and python extensions in vscode
2. Run terminal command "pip install pygame" and "pip install numpy"
3. Run program command "python main.py" or run button
